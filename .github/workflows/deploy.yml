# Ask RGV Backend — build all service images → push to GHCR → SSH deploy on VPS.
#
# Build jobs run in parallel. bge-m3 is skipped unless embeddings/ changed.
# GHA layer cache keeps warm builds under 2 minutes.
#
# Migrations are baked into the askrgv-backend image (migrations/ is copied in
# by the Dockerfile). The app's db/migrations.py runner applies any pending
# migrations at startup — no manual SCP or psql steps needed.
#
# Images built:
#   ghcr.io/marava-tech/askrgv-backend:latest
#   ghcr.io/marava-tech/askrgv-ingestion:latest
#   ghcr.io/marava-tech/askrgv-bge-m3:latest
#
# Secrets required:
#   SERVER_HOST, SERVER_USER, SERVER_SSH_KEY

name: Deploy backend

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

concurrency:
  group: askrgv-backend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v5
        with:
          context: ./
          file: ./app/Dockerfile
          push: true
          tags: ghcr.io/marava-tech/askrgv-backend:latest
          cache-from: type=gha,scope=askrgv-backend
          cache-to: type=gha,mode=max,scope=askrgv-backend

  build-ingestion:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v5
        with:
          context: ./ingestion-worker
          push: true
          tags: ghcr.io/marava-tech/askrgv-ingestion:latest
          cache-from: type=gha,scope=askrgv-ingestion
          cache-to: type=gha,mode=max,scope=askrgv-ingestion

  build-bge-m3:
    runs-on: ubuntu-latest
    outputs:
      rebuilt: ${{ steps.filter.outputs.embeddings }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            embeddings:
              - 'embeddings/**'
      - uses: docker/login-action@v3
        if: steps.filter.outputs.embeddings == 'true'
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/setup-buildx-action@v3
        if: steps.filter.outputs.embeddings == 'true'
      - uses: docker/build-push-action@v5
        if: steps.filter.outputs.embeddings == 'true'
        with:
          context: ./embeddings
          push: true
          tags: ghcr.io/marava-tech/askrgv-bge-m3:latest
          cache-from: type=gha,scope=askrgv-bge-m3
          cache-to: type=gha,mode=max,scope=askrgv-bge-m3

  deploy:
    needs: [build-backend, build-ingestion, build-bge-m3]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check SSH secrets
        env:
          SERVER_HOST_VAL: ${{ secrets.SERVER_HOST }}
          SERVER_USER_VAL: ${{ secrets.SERVER_USER }}
          SERVER_KEY_VAL: ${{ secrets.SERVER_SSH_KEY }}
        run: |
          ok=0
          [ -z "$SERVER_HOST_VAL" ] && echo "::error::Secret SERVER_HOST is missing." && ok=1
          [ -z "$SERVER_USER_VAL" ] && echo "::error::Secret SERVER_USER is missing." && ok=1
          [ -z "$SERVER_KEY_VAL"  ] && echo "::error::Secret SERVER_SSH_KEY is missing." && ok=1
          exit "$ok"

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -T 10 -H "${{ secrets.SERVER_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null \
            || echo "::warning::ssh-keyscan timed out — falling back to StrictHostKeyChecking=accept-new"
          cat >> ~/.ssh/config <<'EOF'
          Host *
            StrictHostKeyChecking accept-new
            ConnectTimeout 30
            ServerAliveInterval 30
            ServerAliveCountMax 3
          EOF

      - name: Deploy on server via SSH
        env:
          BGE_REBUILT: ${{ needs.build-bge-m3.outputs.rebuilt }}
        run: |
          ssh -i ~/.ssh/deploy_key \
            "${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}" \
            "BGE_REBUILT='${BGE_REBUILT}' bash -s" << 'ENDSSH'
          set -euo pipefail
          COMPOSE="docker compose -f /root/server-setup/services/ask-rgv-backend/docker-compose.yml --env-file /root/server-setup/.env"

          $COMPOSE up -d --no-recreate askrgv-postgres askrgv-redis askrgv-qdrant

          for i in $(seq 1 15); do
            docker exec askrgv-postgres pg_isready -U askrgv -d askrgv && break
            echo "Waiting for postgres... ($i/15)"; sleep 2
          done

          pull_with_retry() {
            local service=$1
            for attempt in 1 2 3; do
              echo "Pulling $service (attempt $attempt)..."
              $COMPOSE pull "$service" && return 0
              [ $attempt -lt 3 ] && echo "Retrying in 15s..." && sleep 15
            done
            echo "ERROR: failed to pull $service after 3 attempts" && return 1
          }

          pull_with_retry askrgv-backend
          pull_with_retry askrgv-ingestion

          if [ "${BGE_REBUILT}" = "true" ]; then
            echo "embeddings/ changed — pulling bge-m3"
            pull_with_retry askrgv-bge-m3
            $COMPOSE up -d --force-recreate askrgv-bge-m3
          else
            echo "embeddings/ unchanged — skipping bge-m3 pull"
            if docker ps --filter "name=askrgv-bge-m3" --filter "status=running" -q | grep -q .; then
              echo "askrgv-bge-m3 already running — no action needed"
            else
              $COMPOSE up -d --no-recreate askrgv-bge-m3
            fi
          fi

          # Migrations are baked into the image and applied automatically at startup
          # by db/migrations.py — no manual psql steps needed here.
          $COMPOSE up -d --force-recreate askrgv-backend askrgv-ingestion

          docker image prune -f
          sleep 10
          docker logs askrgv-backend --tail 30
          ENDSSH
